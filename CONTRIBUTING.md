# Contributing

Small project, light process.

1. Fork and branch.
2. `npm install`, then `npm run dev`.
3. Before a PR, run `npm run lint` and `npm run build`.
4. Keep changes focused and say what changed and why.

Issues for bugs and small ideas are welcome.
